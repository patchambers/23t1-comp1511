// Procedure Syntax
return_type function_name(function arguments) {
    code to run inside function
}

// Procedures don't return anything, so the return type is always 'void'
e.g.
void print_hello() {
    printf("hello\n");
}
// The return type is 'void', the function name is 'print_hello' and the
// procedure takes no arguments so the brackets '()' are empty

// To call this procedure (use it later in code), you use the function name 
// and the brackets '()'
e.g. 
print_hello();
